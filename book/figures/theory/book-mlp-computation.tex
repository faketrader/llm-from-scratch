\documentclass[tikz,border=8pt]{standalone}
\usepackage{ctex,amsmath}
\usetikzlibrary{arrows.meta,positioning}
\input{diagram_styles.tex}
\begin{document}
\begin{tikzpicture}[lfs picture,node distance=9mm and 10mm]
\node[storage] (x) {$\mat{X}$\\$B\times d$};
\node[compute,right=of x] (z) {$\mat{X}\mat{W}_1+\vect{b}_1$\\$B\times h$};
\node[compute,right=of z] (a) {$\mat{A}=\operatorname{ReLU}(\mat{Z})$\\$B\times h$};
\node[compute,right=of a] (u) {$\mat{U}=\mat{A}\mat{W}_2+\vect{b}_2$\\$B\times C$};
\node[block,below=of u] (loss) {Softmax 与交叉熵\\标量损失 $\mathcal L$};
\node[storage,left=of loss] (y) {目标 $\mat{Y}$\\$B\times C$};
\draw[->] (x)--(z);\draw[->] (z)--(a);\draw[->] (a)--(u);\draw[->] (u)--(loss);\draw[->] (y)--(loss);
\draw[->,dashed] (loss.east)--++(8mm,0)|-node[above,pos=.7]{反向：$\mat{G}=\frac{\mat{P}-\mat{Y}}{B}$}(u.north);
\draw[->,dashed] (u.north)--++(0,12mm)-|node[above,pos=.25]{$\mat{G}\mat{W}_2^{\mathrm{T}}\odot\mathbf1[\mat{Z}>0]$}(z.north);
\end{tikzpicture}
\end{document}
